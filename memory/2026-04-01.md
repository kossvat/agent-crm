# 2026-04-01 CRM Fixes

## Сделано
1. **Remote Control visibility fix** — заменил `localStorage.getItem('workspace_token')` (никогда не сохранялся) на API check `/connect/has-redeemed`
2. **Copy Setup Message 401 fix** — endpoint `/setup/message` теперь принимает JWT auth и генерирует workspace_token на лету (раньше требовал несуществующий токен из localStorage)
3. **Clipboard fallback** — `navigator.clipboard.writeText()` не работает в Telegram WebView, добавил fallback через textarea + execCommand('copy')
4. **Cache bust** — v20260331d → v20260331e
5. **Cloudflare tunnel убран** — `crm.myaiagentscrm.com` DNS запись удалена Святом. Локальный CRM больше не торчит наружу. Безопаснее.

## Коммиты (feat/multi-tenant)
- `5bea1ff` — hide Remote Control when connect token redeemed
- `6e62d28` — copySetupMessage uses JWT auth
- `5eaf6a8` — clipboard fallback for Telegram WebView
- `c9abd49` — cache bust

## Деплой
- Прод (myaiagentscrm.com) обновлён

## Ключевое решение от Свята
- **CRM = только для юзеров с OpenClaw**. Все фичи (spending, agents, remote control) показывать всем.
- **Навигация 8 табов — оставить как есть** (Home, Board, Agents, Crons, Files, Journal, Alerts, Admin)
- Board/Tasks не трогать
- Tunnel `crm.myaiagentscrm.com` убран, остаётся только `myaiagentscrm.com`

## TODO
- [ ] Onboarding empty state для юзеров без OpenClaw ("Установи OpenClaw → подключи агента")
- [ ] hasOpenClaw проверки пересмотреть — spending показывать всем
- [ ] Мерж feat/multi-tenant → master
